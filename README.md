# StudentTeacherPortal
A role-based full-stack web application where teachers (admin) can assign tasks to all students or specific individual students, and students can manage their personal to-do lists and view tasks assigned by teachers. The application uses Django REST Framework for backend APIs and React for frontend UI, with PostgreSQL as the database.
